        %create noise patch
        noise_level= 0.5;
        %m bewlow comes from createGabors
        noisemat2=rand(legnth(m), length(m))*noise_level+0.5;
        noisemat2(noisemat2>1)=1;
        noisemat2(noisemat2<1)=0;
        noisetex2=Screen('MakeTexture', w, noisemat2, [], [],2);
        
        
        maskRect= Screen ('OpenOffscreenwindow', w, 0.5, maskRect